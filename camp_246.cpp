#include <iostream>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin>>n;
    string s;
    cin>>s;
    int arr[s.length()];
    for(int i=0; i<s.length(); i++){
        if(islower(s[i])){
            arr[i] = 0;
        }else{
            arr[i] = 1;
        }
    }
    vector<int> v;
    for(int i=0;i<s.length(); i++){
        int 
    }
}